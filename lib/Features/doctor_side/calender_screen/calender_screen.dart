import 'package:flutter/material.dart';

class CalenderScreenD extends StatelessWidget {
  const CalenderScreenD({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("calender")));
  }
}
